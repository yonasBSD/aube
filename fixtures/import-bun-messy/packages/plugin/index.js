const core = require('@fixture/core');
const isNumber = require('is-number');
const kindOf = require('kind-of');
const oddAlias = require('odd-alias');

module.exports = { core, isNumber, kindOf, oddAlias };
